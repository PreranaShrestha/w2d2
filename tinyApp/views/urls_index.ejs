<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <%-include('partials/_header.ejs')%>
      <% for(var key in urls) { %>
      <form action = "/urls/<%= key %>/delete" method = "POST" style = "margin:15px;">
          <label for="shortURL"><%= key %></label>
          <a href = <%= urls[key] %> > <%= urls[key] %></a>
          <a href = "urls/<%= key %>/update" > Edit </a>
          <button type="submit" value="Delete">🗑️</button></br>
        </form>
      <% } %>
</body>
</html>